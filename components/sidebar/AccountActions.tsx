// AccountActions.jsx

const AccountActions = () => {
  return (
    // mt-auto: flex 컨테이너 내에서 남는 공간을 모두 차지하여 맨 아래로 이동
    // h-[90px]: 컴포넌트의 높이를 90px로 설정
    <div className="h-[90px] border">AccountActions</div>
  );
};

export default AccountActions;
